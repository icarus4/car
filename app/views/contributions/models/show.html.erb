<h3><%= link_to "<< 回車系列表", contributions_path %></h3>

<hr>

<h3><%= "#{@model.name} 車款列表" %></h3>

<ul>
<% @cars.each do |car| %>
  <li>
  <%= "#{car.displacement} #{car.submodel}" %> (<%= link_to "編輯", edit_contributions_brand_model_car_path(car.model.brand, car.model, car) %>)
  </li>
<% end %>
</ul>
