<h3><%= link_to "<< 回車系列表", contributions_path %></h3>

<hr>

<h3><%= "#{@model.brand.name} #{@model.name} 車款列表" %></h3>

<ul>

  <% @cars.each do |car| %>

  <li>
    <%= "#{car.displacement} #{car.submodel}" %> （<%= link_to "修改", edit_contributions_brand_model_car_path(@model.brand, @model, car) %>）
  </li>

  <% end %>

  <li><%= link_to "新增車款", new_contributions_brand_model_car_path(@model.brand, @model) %></li>
</ul>

<hr>
<div id="car-data-table-div">
  <h4 style='text-align: center'>以下為 <%= "#{@model.brand.name} #{@model.name}" %> 目前的安全配備一覽表</h4>
  <%= render partial: 'common/cars_data_table', locals: {cars: @cars, table_class: 'model-data-table'} %>

</div>
